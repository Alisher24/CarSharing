// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, ServerSentEventsResult, TDataShape } from './client';
import { client } from './client.gen';
import type { CancelRentalData, CancelRentalErrors, CancelRentalResponses, FinishRentalData, FinishRentalErrors, FinishRentalResponses, GetCurrentRentalData, GetCurrentRentalErrors, GetCurrentRentalResponses, GetHealthLiveData, GetHealthLiveErrors, GetHealthLiveResponses, GetHealthReadyData, GetHealthReadyErrors, GetHealthReadyResponses, GetInvoiceData, GetInvoiceErrors, GetInvoiceResponses, GetInvoicesData, GetInvoicesErrors, GetInvoicesResponses, GetMeData, GetMeErrors, GetMeResponses, GetNotificationsData, GetNotificationsErrors, GetNotificationsResponses, GetPrivateEventsData, GetPrivateEventsErrors, GetPrivateEventsResponse, GetPrivateEventsResponses, GetPublicEventsData, GetPublicEventsErrors, GetPublicEventsResponse, GetPublicEventsResponses, GetRidesData, GetRidesErrors, GetRidesResponses, GetTariffsData, GetTariffsErrors, GetTariffsResponses, GetVehicleData, GetVehicleErrors, GetVehicleResponses, GetVehiclesData, GetVehiclesErrors, GetVehiclesResponses, GetZonesData, GetZonesErrors, GetZonesResponses, LoginData, LoginErrors, LoginResponses, LogoutData, LogoutErrors, LogoutResponses, PauseRentalData, PauseRentalErrors, PauseRentalResponses, PayInvoiceData, PayInvoiceErrors, PayInvoiceResponses, ReadNotificationData, ReadNotificationErrors, ReadNotificationResponses, RegisterData, RegisterErrors, RegisterResponses, ReserveData, ReserveErrors, ReserveResponses, ResumeRentalData, ResumeRentalErrors, ResumeRentalResponses, StartRentalData, StartRentalErrors, StartRentalResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * login
 *
 * Rotate the session token. Unknown email and wrong password give the identical INVALID_CREDENTIALS response. Session cookie carsharing_session is HttpOnly, Path=/, SameSite=Lax, with an absolute 12-hour lifetime and no sliding renewal. Secure is required over HTTPS and disabled only for local HTTP. The session token is never in JSON. Origin is checked on browser mutations; authenticated mutations also check X-CSRF-Token. Request body is required.
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>): RequestResult<LoginResponses, LoginErrors, ThrowOnError> => (options.client ?? client).post<LoginResponses, LoginErrors, ThrowOnError>({
    url: '/api/v1/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * logout
 *
 * Naturally idempotent: clear cookie and return 204 when the session is absent or expired. Origin is still checked. For an existing session CSRF is required. Session cookie carsharing_session is HttpOnly, Path=/, SameSite=Lax, with an absolute 12-hour lifetime and no sliding renewal. Secure is required over HTTPS and disabled only for local HTTP. The session token is never in JSON. Origin is checked on browser mutations; authenticated mutations also check X-CSRF-Token. No request body is permitted, including an empty JSON object.
 */
export const logout = <ThrowOnError extends boolean = false>(options: Options<LogoutData, ThrowOnError>): RequestResult<LogoutResponses, LogoutErrors, ThrowOnError> => (options.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({ url: '/api/v1/auth/logout', ...options });

/**
 * register
 *
 * Register and immediately establish a session. Session cookie carsharing_session is HttpOnly, Path=/, SameSite=Lax, with an absolute 12-hour lifetime and no sliding renewal. Secure is required over HTTPS and disabled only for local HTTP. The session token is never in JSON. Origin is checked on browser mutations; authenticated mutations also check X-CSRF-Token. Request body is required.
 */
export const register = <ThrowOnError extends boolean = false>(options: Options<RegisterData, ThrowOnError>): RequestResult<RegisterResponses, RegisterErrors, ThrowOnError> => (options.client ?? client).post<RegisterResponses, RegisterErrors, ThrowOnError>({
    url: '/api/v1/auth/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * get Public Events
 *
 * Ephemeral invalidation signal, not a log. Subscribe then send retry 3000 and ready with server_time. Heartbeat is a keepalive comment every 15 seconds. No SSE id, Last-Event-ID or replay support. Payload is only resource id/version, never full objects or private data. Compare versions within (resource,id), ignore equal/older. Reconnect waits for ready then fetches REST snapshots; also reconcile vehicles/me/current every five seconds. A bounded-queue overflow closes the stream. Losing the session closes the private stream; check me and clear private cache on 401 or reconnect. Events: vehicle.changed, zone.changed, tariff.changed. Anonymous public resources only. No request body is permitted, including an empty JSON object.
 */
export const getPublicEvents = <ThrowOnError extends boolean = false>(options?: Options<GetPublicEventsData, ThrowOnError, GetPublicEventsResponse>): Promise<ServerSentEventsResult<GetPublicEventsResponses>> => (options?.client ?? client).sse.get<GetPublicEventsResponses, GetPublicEventsErrors, ThrowOnError>({ url: '/api/v1/events', ...options });

/**
 * Check the process without querying dependencies
 */
export const getHealthLive = <ThrowOnError extends boolean = false>(options?: Options<GetHealthLiveData, ThrowOnError>): RequestResult<GetHealthLiveResponses, GetHealthLiveErrors, ThrowOnError> => (options?.client ?? client).get<GetHealthLiveResponses, GetHealthLiveErrors, ThrowOnError>({ url: '/api/v1/health/live', ...options });

/**
 * Check PostgreSQL, schema and PostGIS
 */
export const getHealthReady = <ThrowOnError extends boolean = false>(options?: Options<GetHealthReadyData, ThrowOnError>): RequestResult<GetHealthReadyResponses, GetHealthReadyErrors, ThrowOnError> => (options?.client ?? client).get<GetHealthReadyResponses, GetHealthReadyErrors, ThrowOnError>({ url: '/api/v1/health/ready', ...options });

/**
 * get Me
 *
 * Restore the session context after reload. Session cookie carsharing_session is HttpOnly, Path=/, SameSite=Lax, with an absolute 12-hour lifetime and no sliding renewal. Secure is required over HTTPS and disabled only for local HTTP. The session token is never in JSON. Origin is checked on browser mutations; authenticated mutations also check X-CSRF-Token. No request body is permitted, including an empty JSON object.
 */
export const getMe = <ThrowOnError extends boolean = false>(options?: Options<GetMeData, ThrowOnError>): RequestResult<GetMeResponses, GetMeErrors, ThrowOnError> => (options?.client ?? client).get<GetMeResponses, GetMeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me',
    ...options
});

/**
 * get Current Rental
 *
 * Always 200. Absence is explicit; otherwise contains the authoritative current rental and full current vehicle at server_time. No request body is permitted, including an empty JSON object.
 */
export const getCurrentRental = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentRentalData, ThrowOnError>): RequestResult<GetCurrentRentalResponses, GetCurrentRentalErrors, ThrowOnError> => (options?.client ?? client).get<GetCurrentRentalResponses, GetCurrentRentalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/current',
    ...options
});

/**
 * get Private Events
 *
 * Ephemeral invalidation signal, not a log. Subscribe then send retry 3000 and ready with server_time. Heartbeat is a keepalive comment every 15 seconds. No SSE id, Last-Event-ID or replay support. Payload is only resource id/version, never full objects or private data. Compare versions within (resource,id), ignore equal/older. Reconnect waits for ready then fetches REST snapshots; also reconcile vehicles/me/current every five seconds. A bounded-queue overflow closes the stream. Losing the session closes the private stream; check me and clear private cache on 401 or reconnect. Events: rental.changed, invoice.changed, notification.changed. Only resources owned by this session; public events are not duplicated. No request body is permitted, including an empty JSON object.
 */
export const getPrivateEvents = <ThrowOnError extends boolean = false>(options?: Options<GetPrivateEventsData, ThrowOnError, GetPrivateEventsResponse>): Promise<ServerSentEventsResult<GetPrivateEventsResponses>> => (options?.client ?? client).sse.get<GetPrivateEventsResponses, GetPrivateEventsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/events',
    ...options
});

/**
 * get Invoices
 *
 * Forward-only live keyset pagination ordered by (issued_at DESC, id DESC). Cursor binds endpoint, position, user scope and query parameters with HMAC-SHA256; no TTL, key rotation invalidates. All cursor failures are INVALID_CURSOR.  No request body is permitted, including an empty JSON object.
 */
export const getInvoices = <ThrowOnError extends boolean = false>(options?: Options<GetInvoicesData, ThrowOnError>): RequestResult<GetInvoicesResponses, GetInvoicesErrors, ThrowOnError> => (options?.client ?? client).get<GetInvoicesResponses, GetInvoicesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/invoices',
    ...options
});

/**
 * get Invoice
 *
 * No request body is permitted, including an empty JSON object.
 */
export const getInvoice = <ThrowOnError extends boolean = false>(options: Options<GetInvoiceData, ThrowOnError>): RequestResult<GetInvoiceResponses, GetInvoiceErrors, ThrowOnError> => (options.client ?? client).get<GetInvoiceResponses, GetInvoiceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/invoices/{id}',
    ...options
});

/**
 * pay Invoice
 *
 * An accepted demo decline is HTTP 200 PayResult with failed payment. A new key for an already paid invoice returns the existing view without another charge. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. No request body is permitted, including an empty JSON object.
 */
export const payInvoice = <ThrowOnError extends boolean = false>(options: Options<PayInvoiceData, ThrowOnError>): RequestResult<PayInvoiceResponses, PayInvoiceErrors, ThrowOnError> => (options.client ?? client).post<PayInvoiceResponses, PayInvoiceErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/invoices/{id}/pay',
    ...options
});

/**
 * get Notifications
 *
 * Forward-only live keyset pagination ordered by (created_at DESC, id DESC). Cursor binds endpoint, position, user scope and query parameters with HMAC-SHA256; no TTL, key rotation invalidates. All cursor failures are INVALID_CURSOR.  No request body is permitted, including an empty JSON object.
 */
export const getNotifications = <ThrowOnError extends boolean = false>(options?: Options<GetNotificationsData, ThrowOnError>): RequestResult<GetNotificationsResponses, GetNotificationsErrors, ThrowOnError> => (options?.client ?? client).get<GetNotificationsResponses, GetNotificationsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/notifications',
    ...options
});

/**
 * read Notification
 *
 * Repeated reads return the same final notification; no Idempotency-Key required. No request body is permitted, including an empty JSON object.
 */
export const readNotification = <ThrowOnError extends boolean = false>(options: Options<ReadNotificationData, ThrowOnError>): RequestResult<ReadNotificationResponses, ReadNotificationErrors, ThrowOnError> => (options.client ?? client).post<ReadNotificationResponses, ReadNotificationErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/notifications/{id}/read',
    ...options
});

/**
 * get Rides
 *
 * Forward-only live keyset pagination ordered by (completed_at DESC, id DESC). Cursor binds endpoint, position, user scope and query parameters with HMAC-SHA256; no TTL, key rotation invalidates. All cursor failures are INVALID_CURSOR. Only completed rentals with a started ride; exclude cancelled/expired reservations and the current ride. No request body is permitted, including an empty JSON object.
 */
export const getRides = <ThrowOnError extends boolean = false>(options?: Options<GetRidesData, ThrowOnError>): RequestResult<GetRidesResponses, GetRidesErrors, ThrowOnError> => (options?.client ?? client).get<GetRidesResponses, GetRidesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/me/rides',
    ...options
});

/**
 * reserve
 *
 * Reserve for 15 free minutes. Server chooses immutable tariff conditions. Reject all client-supplied time, owner, state, rates, energy and amount. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. Request body is required.
 */
export const reserve = <ThrowOnError extends boolean = false>(options: Options<ReserveData, ThrowOnError>): RequestResult<ReserveResponses, ReserveErrors, ThrowOnError> => (options.client ?? client).post<ReserveResponses, ReserveErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/reservations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * cancel Rental
 *
 * Reconcile with server time before transition; persist expiry or depletion even if the command conflicts. On 409 reread the authoritative current snapshot. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. No request body is permitted, including an empty JSON object.
 */
export const cancelRental = <ThrowOnError extends boolean = false>(options: Options<CancelRentalData, ThrowOnError>): RequestResult<CancelRentalResponses, CancelRentalErrors, ThrowOnError> => (options.client ?? client).post<CancelRentalResponses, CancelRentalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/reservations/{id}/cancel',
    ...options
});

/**
 * start Rental
 *
 * Reconcile with server time before transition; persist expiry or depletion even if the command conflicts. On 409 reread the authoritative current snapshot. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. No request body is permitted, including an empty JSON object.
 */
export const startRental = <ThrowOnError extends boolean = false>(options: Options<StartRentalData, ThrowOnError>): RequestResult<StartRentalResponses, StartRentalErrors, ThrowOnError> => (options.client ?? client).post<StartRentalResponses, StartRentalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/reservations/{id}/start',
    ...options
});

/**
 * finish Rental
 *
 * Use confirmed server telemetry, never a client finish coordinate. Reconcile first: energy depletion returns 200 with actual completion and invoice. A new key for a completed ride returns the existing rental and invoice. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. No request body is permitted, including an empty JSON object.
 */
export const finishRental = <ThrowOnError extends boolean = false>(options: Options<FinishRentalData, ThrowOnError>): RequestResult<FinishRentalResponses, FinishRentalErrors, ThrowOnError> => (options.client ?? client).post<FinishRentalResponses, FinishRentalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/rides/{id}/finish',
    ...options
});

/**
 * pause Rental
 *
 * Reconcile with server time before transition; persist expiry or depletion even if the command conflicts. On 409 reread the authoritative current snapshot. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. No request body is permitted, including an empty JSON object.
 */
export const pauseRental = <ThrowOnError extends boolean = false>(options: Options<PauseRentalData, ThrowOnError>): RequestResult<PauseRentalResponses, PauseRentalErrors, ThrowOnError> => (options.client ?? client).post<PauseRentalResponses, PauseRentalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/rides/{id}/pause',
    ...options
});

/**
 * resume Rental
 *
 * Reconcile with server time before transition; persist expiry or depletion even if the command conflicts. On 409 reread the authoritative current snapshot. Idempotency scope is the current user; fingerprint is method, normalized path including resource ID and canonical body. Missing key: 400 IDEMPOTENCY_KEY_REQUIRED; invalid UUID v4: 400 IDEMPOTENCY_KEY_INVALID. Same key with a different fingerprint: 409 IDEMPOTENCY_CONFLICT. Concurrent request: 409 IDEMPOTENCY_IN_PROGRESS and Retry-After 1. Replay returns original status/body, server_time and body request_id with Idempotency-Replayed true and a new X-Request-ID for this attempt. Retain successes and checked domain failures for at least 24 hours; exclude auth, validation, in-flight and technical rollbacks. Cleanup cannot remove invoice/payment uniqueness. No request body is permitted, including an empty JSON object.
 */
export const resumeRental = <ThrowOnError extends boolean = false>(options: Options<ResumeRentalData, ThrowOnError>): RequestResult<ResumeRentalResponses, ResumeRentalErrors, ThrowOnError> => (options.client ?? client).post<ResumeRentalResponses, ResumeRentalErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'carsharing_session',
            type: 'apiKey'
        }],
    url: '/api/v1/rides/{id}/resume',
    ...options
});

/**
 * get Tariffs
 *
 * Current tariffs only; no public version history. No request body is permitted, including an empty JSON object.
 */
export const getTariffs = <ThrowOnError extends boolean = false>(options?: Options<GetTariffsData, ThrowOnError>): RequestResult<GetTariffsResponses, GetTariffsErrors, ThrowOnError> => (options?.client ?? client).get<GetTariffsResponses, GetTariffsErrors, ThrowOnError>({ url: '/api/v1/tariffs', ...options });

/**
 * get Vehicles
 *
 * Complete unpaginated demo fleet snapshot. Status and powertrain filters belong to the frontend. No request body is permitted, including an empty JSON object.
 */
export const getVehicles = <ThrowOnError extends boolean = false>(options?: Options<GetVehiclesData, ThrowOnError>): RequestResult<GetVehiclesResponses, GetVehiclesErrors, ThrowOnError> => (options?.client ?? client).get<GetVehiclesResponses, GetVehiclesErrors, ThrowOnError>({ url: '/api/v1/vehicles', ...options });

/**
 * get Vehicle
 *
 * No request body is permitted, including an empty JSON object.
 */
export const getVehicle = <ThrowOnError extends boolean = false>(options: Options<GetVehicleData, ThrowOnError>): RequestResult<GetVehicleResponses, GetVehicleErrors, ThrowOnError> => (options.client ?? client).get<GetVehicleResponses, GetVehicleErrors, ThrowOnError>({ url: '/api/v1/vehicles/{id}', ...options });

/**
 * get Zones
 *
 * Current zones only; no public version history. No request body is permitted, including an empty JSON object.
 */
export const getZones = <ThrowOnError extends boolean = false>(options?: Options<GetZonesData, ThrowOnError>): RequestResult<GetZonesResponses, GetZonesErrors, ThrowOnError> => (options?.client ?? client).get<GetZonesResponses, GetZonesErrors, ThrowOnError>({ url: '/api/v1/zones', ...options });
